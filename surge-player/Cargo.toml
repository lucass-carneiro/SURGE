[package]
name = "surge-player"
version = "1.4.0"
edition = "2024"


[dependencies]
env_logger = "0.11.8"
log = "0.4.28"
surge-core = { path = "../surge-core" }
surge-hot-reload = { path = "../surge-hot-reload" }
surge-mod-default = { path = "../surge-modules/surge-mod-default"}

[features]
default = []
hot_reloading = []
